import { HasPermisionDirective } from './has-permision.directive';

describe('HasPermisionDirective', () => {
  it('should create an instance', () => {
    const directive = new HasPermisionDirective();
    expect(directive).toBeTruthy();
  });
});
